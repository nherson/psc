// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/nherson/psc/api/ent/fight"
	"github.com/nherson/psc/api/ent/fighter"
	"github.com/nherson/psc/api/ent/fighterresults"
	"github.com/nherson/psc/api/ent/predicate"
)

// FighterResultsQuery is the builder for querying FighterResults entities.
type FighterResultsQuery struct {
	config
	ctx         *QueryContext
	order       []fighterresults.Order
	inters      []Interceptor
	predicates  []predicate.FighterResults
	withFighter *FighterQuery
	withFight   *FightQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FighterResultsQuery builder.
func (frq *FighterResultsQuery) Where(ps ...predicate.FighterResults) *FighterResultsQuery {
	frq.predicates = append(frq.predicates, ps...)
	return frq
}

// Limit the number of records to be returned by this query.
func (frq *FighterResultsQuery) Limit(limit int) *FighterResultsQuery {
	frq.ctx.Limit = &limit
	return frq
}

// Offset to start from.
func (frq *FighterResultsQuery) Offset(offset int) *FighterResultsQuery {
	frq.ctx.Offset = &offset
	return frq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (frq *FighterResultsQuery) Unique(unique bool) *FighterResultsQuery {
	frq.ctx.Unique = &unique
	return frq
}

// Order specifies how the records should be ordered.
func (frq *FighterResultsQuery) Order(o ...fighterresults.Order) *FighterResultsQuery {
	frq.order = append(frq.order, o...)
	return frq
}

// QueryFighter chains the current query on the "fighter" edge.
func (frq *FighterResultsQuery) QueryFighter() *FighterQuery {
	query := (&FighterClient{config: frq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := frq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := frq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fighterresults.Table, fighterresults.FieldID, selector),
			sqlgraph.To(fighter.Table, fighter.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, fighterresults.FighterTable, fighterresults.FighterColumn),
		)
		fromU = sqlgraph.SetNeighbors(frq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryFight chains the current query on the "fight" edge.
func (frq *FighterResultsQuery) QueryFight() *FightQuery {
	query := (&FightClient{config: frq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := frq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := frq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fighterresults.Table, fighterresults.FieldID, selector),
			sqlgraph.To(fight.Table, fight.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, fighterresults.FightTable, fighterresults.FightColumn),
		)
		fromU = sqlgraph.SetNeighbors(frq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FighterResults entity from the query.
// Returns a *NotFoundError when no FighterResults was found.
func (frq *FighterResultsQuery) First(ctx context.Context) (*FighterResults, error) {
	nodes, err := frq.Limit(1).All(setContextOp(ctx, frq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fighterresults.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (frq *FighterResultsQuery) FirstX(ctx context.Context) *FighterResults {
	node, err := frq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FighterResults ID from the query.
// Returns a *NotFoundError when no FighterResults ID was found.
func (frq *FighterResultsQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = frq.Limit(1).IDs(setContextOp(ctx, frq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fighterresults.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (frq *FighterResultsQuery) FirstIDX(ctx context.Context) int {
	id, err := frq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FighterResults entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FighterResults entity is found.
// Returns a *NotFoundError when no FighterResults entities are found.
func (frq *FighterResultsQuery) Only(ctx context.Context) (*FighterResults, error) {
	nodes, err := frq.Limit(2).All(setContextOp(ctx, frq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fighterresults.Label}
	default:
		return nil, &NotSingularError{fighterresults.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (frq *FighterResultsQuery) OnlyX(ctx context.Context) *FighterResults {
	node, err := frq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FighterResults ID in the query.
// Returns a *NotSingularError when more than one FighterResults ID is found.
// Returns a *NotFoundError when no entities are found.
func (frq *FighterResultsQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = frq.Limit(2).IDs(setContextOp(ctx, frq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fighterresults.Label}
	default:
		err = &NotSingularError{fighterresults.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (frq *FighterResultsQuery) OnlyIDX(ctx context.Context) int {
	id, err := frq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FighterResultsSlice.
func (frq *FighterResultsQuery) All(ctx context.Context) ([]*FighterResults, error) {
	ctx = setContextOp(ctx, frq.ctx, "All")
	if err := frq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FighterResults, *FighterResultsQuery]()
	return withInterceptors[[]*FighterResults](ctx, frq, qr, frq.inters)
}

// AllX is like All, but panics if an error occurs.
func (frq *FighterResultsQuery) AllX(ctx context.Context) []*FighterResults {
	nodes, err := frq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FighterResults IDs.
func (frq *FighterResultsQuery) IDs(ctx context.Context) (ids []int, err error) {
	if frq.ctx.Unique == nil && frq.path != nil {
		frq.Unique(true)
	}
	ctx = setContextOp(ctx, frq.ctx, "IDs")
	if err = frq.Select(fighterresults.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (frq *FighterResultsQuery) IDsX(ctx context.Context) []int {
	ids, err := frq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (frq *FighterResultsQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, frq.ctx, "Count")
	if err := frq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, frq, querierCount[*FighterResultsQuery](), frq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (frq *FighterResultsQuery) CountX(ctx context.Context) int {
	count, err := frq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (frq *FighterResultsQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, frq.ctx, "Exist")
	switch _, err := frq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (frq *FighterResultsQuery) ExistX(ctx context.Context) bool {
	exist, err := frq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FighterResultsQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (frq *FighterResultsQuery) Clone() *FighterResultsQuery {
	if frq == nil {
		return nil
	}
	return &FighterResultsQuery{
		config:      frq.config,
		ctx:         frq.ctx.Clone(),
		order:       append([]fighterresults.Order{}, frq.order...),
		inters:      append([]Interceptor{}, frq.inters...),
		predicates:  append([]predicate.FighterResults{}, frq.predicates...),
		withFighter: frq.withFighter.Clone(),
		withFight:   frq.withFight.Clone(),
		// clone intermediate query.
		sql:  frq.sql.Clone(),
		path: frq.path,
	}
}

// WithFighter tells the query-builder to eager-load the nodes that are connected to
// the "fighter" edge. The optional arguments are used to configure the query builder of the edge.
func (frq *FighterResultsQuery) WithFighter(opts ...func(*FighterQuery)) *FighterResultsQuery {
	query := (&FighterClient{config: frq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	frq.withFighter = query
	return frq
}

// WithFight tells the query-builder to eager-load the nodes that are connected to
// the "fight" edge. The optional arguments are used to configure the query builder of the edge.
func (frq *FighterResultsQuery) WithFight(opts ...func(*FightQuery)) *FighterResultsQuery {
	query := (&FightClient{config: frq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	frq.withFight = query
	return frq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		FighterID int `json:"fighter_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FighterResults.Query().
//		GroupBy(fighterresults.FieldFighterID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (frq *FighterResultsQuery) GroupBy(field string, fields ...string) *FighterResultsGroupBy {
	frq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FighterResultsGroupBy{build: frq}
	grbuild.flds = &frq.ctx.Fields
	grbuild.label = fighterresults.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		FighterID int `json:"fighter_id,omitempty"`
//	}
//
//	client.FighterResults.Query().
//		Select(fighterresults.FieldFighterID).
//		Scan(ctx, &v)
func (frq *FighterResultsQuery) Select(fields ...string) *FighterResultsSelect {
	frq.ctx.Fields = append(frq.ctx.Fields, fields...)
	sbuild := &FighterResultsSelect{FighterResultsQuery: frq}
	sbuild.label = fighterresults.Label
	sbuild.flds, sbuild.scan = &frq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FighterResultsSelect configured with the given aggregations.
func (frq *FighterResultsQuery) Aggregate(fns ...AggregateFunc) *FighterResultsSelect {
	return frq.Select().Aggregate(fns...)
}

func (frq *FighterResultsQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range frq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, frq); err != nil {
				return err
			}
		}
	}
	for _, f := range frq.ctx.Fields {
		if !fighterresults.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if frq.path != nil {
		prev, err := frq.path(ctx)
		if err != nil {
			return err
		}
		frq.sql = prev
	}
	return nil
}

func (frq *FighterResultsQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FighterResults, error) {
	var (
		nodes       = []*FighterResults{}
		_spec       = frq.querySpec()
		loadedTypes = [2]bool{
			frq.withFighter != nil,
			frq.withFight != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FighterResults).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FighterResults{config: frq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, frq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := frq.withFighter; query != nil {
		if err := frq.loadFighter(ctx, query, nodes, nil,
			func(n *FighterResults, e *Fighter) { n.Edges.Fighter = e }); err != nil {
			return nil, err
		}
	}
	if query := frq.withFight; query != nil {
		if err := frq.loadFight(ctx, query, nodes, nil,
			func(n *FighterResults, e *Fight) { n.Edges.Fight = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (frq *FighterResultsQuery) loadFighter(ctx context.Context, query *FighterQuery, nodes []*FighterResults, init func(*FighterResults), assign func(*FighterResults, *Fighter)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FighterResults)
	for i := range nodes {
		fk := nodes[i].FighterID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(fighter.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fighter_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (frq *FighterResultsQuery) loadFight(ctx context.Context, query *FightQuery, nodes []*FighterResults, init func(*FighterResults), assign func(*FighterResults, *Fight)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FighterResults)
	for i := range nodes {
		fk := nodes[i].FightID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(fight.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fight_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (frq *FighterResultsQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := frq.querySpec()
	_spec.Node.Columns = frq.ctx.Fields
	if len(frq.ctx.Fields) > 0 {
		_spec.Unique = frq.ctx.Unique != nil && *frq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, frq.driver, _spec)
}

func (frq *FighterResultsQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fighterresults.Table, fighterresults.Columns, sqlgraph.NewFieldSpec(fighterresults.FieldID, field.TypeInt))
	_spec.From = frq.sql
	if unique := frq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if frq.path != nil {
		_spec.Unique = true
	}
	if fields := frq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fighterresults.FieldID)
		for i := range fields {
			if fields[i] != fighterresults.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if frq.withFighter != nil {
			_spec.Node.AddColumnOnce(fighterresults.FieldFighterID)
		}
		if frq.withFight != nil {
			_spec.Node.AddColumnOnce(fighterresults.FieldFightID)
		}
	}
	if ps := frq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := frq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := frq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := frq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (frq *FighterResultsQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(frq.driver.Dialect())
	t1 := builder.Table(fighterresults.Table)
	columns := frq.ctx.Fields
	if len(columns) == 0 {
		columns = fighterresults.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if frq.sql != nil {
		selector = frq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if frq.ctx.Unique != nil && *frq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range frq.predicates {
		p(selector)
	}
	for _, p := range frq.order {
		p(selector)
	}
	if offset := frq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := frq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FighterResultsGroupBy is the group-by builder for FighterResults entities.
type FighterResultsGroupBy struct {
	selector
	build *FighterResultsQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (frgb *FighterResultsGroupBy) Aggregate(fns ...AggregateFunc) *FighterResultsGroupBy {
	frgb.fns = append(frgb.fns, fns...)
	return frgb
}

// Scan applies the selector query and scans the result into the given value.
func (frgb *FighterResultsGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, frgb.build.ctx, "GroupBy")
	if err := frgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FighterResultsQuery, *FighterResultsGroupBy](ctx, frgb.build, frgb, frgb.build.inters, v)
}

func (frgb *FighterResultsGroupBy) sqlScan(ctx context.Context, root *FighterResultsQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(frgb.fns))
	for _, fn := range frgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*frgb.flds)+len(frgb.fns))
		for _, f := range *frgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*frgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := frgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FighterResultsSelect is the builder for selecting fields of FighterResults entities.
type FighterResultsSelect struct {
	*FighterResultsQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (frs *FighterResultsSelect) Aggregate(fns ...AggregateFunc) *FighterResultsSelect {
	frs.fns = append(frs.fns, fns...)
	return frs
}

// Scan applies the selector query and scans the result into the given value.
func (frs *FighterResultsSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, frs.ctx, "Select")
	if err := frs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FighterResultsQuery, *FighterResultsSelect](ctx, frs.FighterResultsQuery, frs, frs.inters, v)
}

func (frs *FighterResultsSelect) sqlScan(ctx context.Context, root *FighterResultsQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(frs.fns))
	for _, fn := range frs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*frs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := frs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
