// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/nherson/psc/api/ent/fighter"
	"github.com/nherson/psc/api/ent/fighteralias"
	"github.com/nherson/psc/api/ent/predicate"
)

// FighterAliasQuery is the builder for querying FighterAlias entities.
type FighterAliasQuery struct {
	config
	ctx         *QueryContext
	order       []fighteralias.Order
	inters      []Interceptor
	predicates  []predicate.FighterAlias
	withFighter *FighterQuery
	withFKs     bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FighterAliasQuery builder.
func (faq *FighterAliasQuery) Where(ps ...predicate.FighterAlias) *FighterAliasQuery {
	faq.predicates = append(faq.predicates, ps...)
	return faq
}

// Limit the number of records to be returned by this query.
func (faq *FighterAliasQuery) Limit(limit int) *FighterAliasQuery {
	faq.ctx.Limit = &limit
	return faq
}

// Offset to start from.
func (faq *FighterAliasQuery) Offset(offset int) *FighterAliasQuery {
	faq.ctx.Offset = &offset
	return faq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (faq *FighterAliasQuery) Unique(unique bool) *FighterAliasQuery {
	faq.ctx.Unique = &unique
	return faq
}

// Order specifies how the records should be ordered.
func (faq *FighterAliasQuery) Order(o ...fighteralias.Order) *FighterAliasQuery {
	faq.order = append(faq.order, o...)
	return faq
}

// QueryFighter chains the current query on the "fighter" edge.
func (faq *FighterAliasQuery) QueryFighter() *FighterQuery {
	query := (&FighterClient{config: faq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := faq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := faq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(fighteralias.Table, fighteralias.FieldID, selector),
			sqlgraph.To(fighter.Table, fighter.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, fighteralias.FighterTable, fighteralias.FighterColumn),
		)
		fromU = sqlgraph.SetNeighbors(faq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FighterAlias entity from the query.
// Returns a *NotFoundError when no FighterAlias was found.
func (faq *FighterAliasQuery) First(ctx context.Context) (*FighterAlias, error) {
	nodes, err := faq.Limit(1).All(setContextOp(ctx, faq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{fighteralias.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (faq *FighterAliasQuery) FirstX(ctx context.Context) *FighterAlias {
	node, err := faq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FighterAlias ID from the query.
// Returns a *NotFoundError when no FighterAlias ID was found.
func (faq *FighterAliasQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = faq.Limit(1).IDs(setContextOp(ctx, faq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{fighteralias.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (faq *FighterAliasQuery) FirstIDX(ctx context.Context) int {
	id, err := faq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FighterAlias entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FighterAlias entity is found.
// Returns a *NotFoundError when no FighterAlias entities are found.
func (faq *FighterAliasQuery) Only(ctx context.Context) (*FighterAlias, error) {
	nodes, err := faq.Limit(2).All(setContextOp(ctx, faq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{fighteralias.Label}
	default:
		return nil, &NotSingularError{fighteralias.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (faq *FighterAliasQuery) OnlyX(ctx context.Context) *FighterAlias {
	node, err := faq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FighterAlias ID in the query.
// Returns a *NotSingularError when more than one FighterAlias ID is found.
// Returns a *NotFoundError when no entities are found.
func (faq *FighterAliasQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = faq.Limit(2).IDs(setContextOp(ctx, faq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{fighteralias.Label}
	default:
		err = &NotSingularError{fighteralias.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (faq *FighterAliasQuery) OnlyIDX(ctx context.Context) int {
	id, err := faq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FighterAliasSlice.
func (faq *FighterAliasQuery) All(ctx context.Context) ([]*FighterAlias, error) {
	ctx = setContextOp(ctx, faq.ctx, "All")
	if err := faq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FighterAlias, *FighterAliasQuery]()
	return withInterceptors[[]*FighterAlias](ctx, faq, qr, faq.inters)
}

// AllX is like All, but panics if an error occurs.
func (faq *FighterAliasQuery) AllX(ctx context.Context) []*FighterAlias {
	nodes, err := faq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FighterAlias IDs.
func (faq *FighterAliasQuery) IDs(ctx context.Context) (ids []int, err error) {
	if faq.ctx.Unique == nil && faq.path != nil {
		faq.Unique(true)
	}
	ctx = setContextOp(ctx, faq.ctx, "IDs")
	if err = faq.Select(fighteralias.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (faq *FighterAliasQuery) IDsX(ctx context.Context) []int {
	ids, err := faq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (faq *FighterAliasQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, faq.ctx, "Count")
	if err := faq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, faq, querierCount[*FighterAliasQuery](), faq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (faq *FighterAliasQuery) CountX(ctx context.Context) int {
	count, err := faq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (faq *FighterAliasQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, faq.ctx, "Exist")
	switch _, err := faq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (faq *FighterAliasQuery) ExistX(ctx context.Context) bool {
	exist, err := faq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FighterAliasQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (faq *FighterAliasQuery) Clone() *FighterAliasQuery {
	if faq == nil {
		return nil
	}
	return &FighterAliasQuery{
		config:      faq.config,
		ctx:         faq.ctx.Clone(),
		order:       append([]fighteralias.Order{}, faq.order...),
		inters:      append([]Interceptor{}, faq.inters...),
		predicates:  append([]predicate.FighterAlias{}, faq.predicates...),
		withFighter: faq.withFighter.Clone(),
		// clone intermediate query.
		sql:  faq.sql.Clone(),
		path: faq.path,
	}
}

// WithFighter tells the query-builder to eager-load the nodes that are connected to
// the "fighter" edge. The optional arguments are used to configure the query builder of the edge.
func (faq *FighterAliasQuery) WithFighter(opts ...func(*FighterQuery)) *FighterAliasQuery {
	query := (&FighterClient{config: faq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	faq.withFighter = query
	return faq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FighterAlias.Query().
//		GroupBy(fighteralias.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (faq *FighterAliasQuery) GroupBy(field string, fields ...string) *FighterAliasGroupBy {
	faq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FighterAliasGroupBy{build: faq}
	grbuild.flds = &faq.ctx.Fields
	grbuild.label = fighteralias.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.FighterAlias.Query().
//		Select(fighteralias.FieldCreatedAt).
//		Scan(ctx, &v)
func (faq *FighterAliasQuery) Select(fields ...string) *FighterAliasSelect {
	faq.ctx.Fields = append(faq.ctx.Fields, fields...)
	sbuild := &FighterAliasSelect{FighterAliasQuery: faq}
	sbuild.label = fighteralias.Label
	sbuild.flds, sbuild.scan = &faq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FighterAliasSelect configured with the given aggregations.
func (faq *FighterAliasQuery) Aggregate(fns ...AggregateFunc) *FighterAliasSelect {
	return faq.Select().Aggregate(fns...)
}

func (faq *FighterAliasQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range faq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, faq); err != nil {
				return err
			}
		}
	}
	for _, f := range faq.ctx.Fields {
		if !fighteralias.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if faq.path != nil {
		prev, err := faq.path(ctx)
		if err != nil {
			return err
		}
		faq.sql = prev
	}
	return nil
}

func (faq *FighterAliasQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FighterAlias, error) {
	var (
		nodes       = []*FighterAlias{}
		withFKs     = faq.withFKs
		_spec       = faq.querySpec()
		loadedTypes = [1]bool{
			faq.withFighter != nil,
		}
	)
	if faq.withFighter != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, fighteralias.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FighterAlias).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FighterAlias{config: faq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, faq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := faq.withFighter; query != nil {
		if err := faq.loadFighter(ctx, query, nodes, nil,
			func(n *FighterAlias, e *Fighter) { n.Edges.Fighter = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (faq *FighterAliasQuery) loadFighter(ctx context.Context, query *FighterQuery, nodes []*FighterAlias, init func(*FighterAlias), assign func(*FighterAlias, *Fighter)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*FighterAlias)
	for i := range nodes {
		if nodes[i].fighter_fighter_aliases == nil {
			continue
		}
		fk := *nodes[i].fighter_fighter_aliases
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(fighter.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "fighter_fighter_aliases" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (faq *FighterAliasQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := faq.querySpec()
	_spec.Node.Columns = faq.ctx.Fields
	if len(faq.ctx.Fields) > 0 {
		_spec.Unique = faq.ctx.Unique != nil && *faq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, faq.driver, _spec)
}

func (faq *FighterAliasQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(fighteralias.Table, fighteralias.Columns, sqlgraph.NewFieldSpec(fighteralias.FieldID, field.TypeInt))
	_spec.From = faq.sql
	if unique := faq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if faq.path != nil {
		_spec.Unique = true
	}
	if fields := faq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, fighteralias.FieldID)
		for i := range fields {
			if fields[i] != fighteralias.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := faq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := faq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := faq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := faq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (faq *FighterAliasQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(faq.driver.Dialect())
	t1 := builder.Table(fighteralias.Table)
	columns := faq.ctx.Fields
	if len(columns) == 0 {
		columns = fighteralias.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if faq.sql != nil {
		selector = faq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if faq.ctx.Unique != nil && *faq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range faq.predicates {
		p(selector)
	}
	for _, p := range faq.order {
		p(selector)
	}
	if offset := faq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := faq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FighterAliasGroupBy is the group-by builder for FighterAlias entities.
type FighterAliasGroupBy struct {
	selector
	build *FighterAliasQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fagb *FighterAliasGroupBy) Aggregate(fns ...AggregateFunc) *FighterAliasGroupBy {
	fagb.fns = append(fagb.fns, fns...)
	return fagb
}

// Scan applies the selector query and scans the result into the given value.
func (fagb *FighterAliasGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fagb.build.ctx, "GroupBy")
	if err := fagb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FighterAliasQuery, *FighterAliasGroupBy](ctx, fagb.build, fagb, fagb.build.inters, v)
}

func (fagb *FighterAliasGroupBy) sqlScan(ctx context.Context, root *FighterAliasQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fagb.fns))
	for _, fn := range fagb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fagb.flds)+len(fagb.fns))
		for _, f := range *fagb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fagb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fagb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FighterAliasSelect is the builder for selecting fields of FighterAlias entities.
type FighterAliasSelect struct {
	*FighterAliasQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fas *FighterAliasSelect) Aggregate(fns ...AggregateFunc) *FighterAliasSelect {
	fas.fns = append(fas.fns, fns...)
	return fas
}

// Scan applies the selector query and scans the result into the given value.
func (fas *FighterAliasSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fas.ctx, "Select")
	if err := fas.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FighterAliasQuery, *FighterAliasSelect](ctx, fas.FighterAliasQuery, fas, fas.inters, v)
}

func (fas *FighterAliasSelect) sqlScan(ctx context.Context, root *FighterAliasQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fas.fns))
	for _, fn := range fas.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fas.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fas.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
